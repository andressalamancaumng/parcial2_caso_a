# Base de datos
DATABASE_URL=postgresql+asyncpg://clinica_user:CHANGE_ME@10.0.0.12:5432/clinica_db

# JWT — usar openssl rand -hex 32 para generar
JWT_SECRET_KEY=CHANGE_ME_USE_OPENSSL_RAND_HEX_32
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=60

# Entorno
ENVIRONMENT=development
